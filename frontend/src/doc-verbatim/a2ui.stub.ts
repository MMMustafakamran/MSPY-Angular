// Empty on purpose. src/main.ts imports this in every build; the
// `doc-a2ui` configuration in angular.json swaps it for ./a2ui.ts, the A2UI
// guide's three code blocks verbatim, so `ng build --configuration doc-a2ui`
// shows what the guide's code does when compiled. The normal build is unchanged.
export {};
